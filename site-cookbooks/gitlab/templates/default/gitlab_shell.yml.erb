# GitLab user. git by default
user: vagrant

# Url to gitlab instance. Used for api calls
gitlab_url: "http://localhost/"

# Repositories path
repos_path: "<%= @repo_path %>"

# File used as authorized_keys for gitlab user
auth_file: "<%= @keys_path %>"

# Error log
error_log: "<%= @shell_log_file %>"
